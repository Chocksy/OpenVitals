/**
 * `/m/[code]` folded into Blood in phase 30a. The marker page is rebuilt in
 * phase 30c; until then this route renders the old page so the redirect from
 * `/m/:code` has somewhere to land.
 */
export { default } from "../../../m/[code]/page";
